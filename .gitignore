/.claude/

# Bí mật — `.env.example` là bản mẫu và ĐƯỢC commit; `.env` là bản thật và không bao giờ commit.
.env
.env.*
!.env.example

# Node / Next
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Test
test-results/
playwright-report/
