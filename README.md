📦 Sokoban — every level generated by machine, and proven solvable before you see it

A Sokoban built with Next.js. No level in this game was drawn by hand: each one is
assembled by a generator, then handed to a solver that has to find the optimal solution
before the level is allowed to exist. That is also where the number on the HUD comes
from — you always know how few pushes the level can be done in. No server, no sign-in:
your records stay on your own device.

**Play**: https://levananhduc.github.io/web-game-sokoban/

## Features

- **The game itself**

  - Walk into a crate to push it; a crate cannot be pushed into a wall or into another crate
  - Undo and redo step through the history you actually played, so a wrong push costs seconds instead of a restart
  - The move counter and the push counter are separate, because in Sokoban only pushes are hard

- **Levels that never run out**

  - A campaign of levels across four tiers, all generated and all verified solvable
  - A random level button generates a fresh one in the browser, in a Web Worker, so the page never freezes while it thinks
  - Every level carries its seed in the URL — send the link and the other person gets exactly that level

- **You always know what a perfect run looks like**

  - The HUD shows the optimal push count next to your own, from the moment the level opens
  - Matching it marks the level with a star; beating your old record replaces it, a worse run never does
  - Records, progress and the game you left half-finished survive closing the tab

- **It tells you when a crate is dead**

  - Push a crate into a corner it can never leave and a strip appears under the board offering the undo
  - The check is deliberately cautious: it never cries wolf on a position that can still be won
  - It is a strip and not a dialog, because a dialog would cover the undo button you are reaching for

- **Built for a phone as much as a keyboard**

  - Arrow keys or WASD to move, `Z` to undo, `R` to restart; arrow keys never scroll the page
  - Swipe or use the on-screen D-pad on touch devices — the D-pad only appears where there is no mouse
  - The board resizes to fit the screen instead of scrolling, so the whole puzzle is always visible

- **Readable for everyone**

  - Every board state differs in shape as well as colour: goals are rings, crates are braced squares, a finished crate gains a check
  - Light and dark themes follow the system, or you can pin one
  - `prefers-reduced-motion` removes animation entirely rather than shortening it

- **A trail of where you have been**

  - Your last twelve steps stay on the floor as fading dots
  - Sokoban is a game about the path you took, and undo is about the path you regret — seeing it makes "where did I go wrong" answerable without replaying the level in your head

## Running it

```bash
yarn install
yarn dev          # http://localhost:3000
```

No environment variables are needed to run or develop — see [`.env.example`](.env.example).

```bash
yarn test         # unit tests, including re-solving every committed level
yarn test:e2e     # Playwright, against the real static build
yarn typecheck
yarn lint
yarn build        # static export into out/
```

Regenerating the level packs (they are committed, so this is only needed when the
generator changes):

```bash
yarn levels:generate            # all four tiers
yarn levels:generate hard 5     # just five hard levels
```

The generator is deterministic: the same root seed produces the same pack, which is why
the committed JSON can be verified rather than trusted.

## Releases and versioning

Every push to `main` creates a GitHub Release by itself
([`.github/workflows/release.yml`](.github/workflows/release.yml)), and deploys to
**<https://levananhduc.github.io/web-game-sokoban/>**
([`deploy.yml`](.github/workflows/deploy.yml)). Pull requests run typecheck, lint, unit
tests, a build, the browser suite and a dependency audit first
([`ci.yml`](.github/workflows/ci.yml)).

Pages has to be enabled **once per repository** before the first deploy can succeed —
`configure-pages` cannot do it for you, because `GITHUB_TOKEN` is not allowed to create a
Pages site:

```bash
gh api -X POST repos/LeVanAnhDuc/web-game-sokoban/pages -f build_type=workflow
```

**The version comes from your commit subjects**, so they have to follow Conventional
Commits. The whole range since the previous tag is scanned, so one `feat:` anywhere in a
push is enough for a minor bump — the merge commit's own subject does not need a prefix.

| In the range since the last tag | Bump |
| --- | --- |
| `feat:` | minor — `v0.4.0` → `v0.5.0` |
| `fix:` · `docs:` · `chore:` · `ci:` · `refactor:` · `test:` · `perf:` | patch — `v0.4.0` → `v0.4.1` |
| `feat!:` (any `type!:`) or a `BREAKING CHANGE` footer | see the 0.x rule below |

**The 0.x rule:** while the major version is `0`, a breaking change bumps the **minor**,
not the major. Nothing is stable before 1.0, and `1.0.0` is a claim about completeness —
so crossing to it takes the explicit marker rather than happening on its own. The first
release of this repo is `v0.1.0` for the same reason.

Three markers, honoured **only in the HEAD commit subject** (not in bodies — the bodies
here run long and discuss releases, which would otherwise trigger them):

- `[release minor]` / `[release major]` — force a bigger bump. `[release major]` is the
  only way to reach `1.0.0`.
- `[skip release]` — no release for this push. For CI-only changes where a release would
  be noise. Never use it on a push that also carries a `feat:`, or you cancel that
  feature's release too.

**The notes are composed from the commit subjects**, grouped by type, breaking changes
first — see [`release-notes.sh`](.github/scripts/release-notes.sh). Not from
`--generate-notes`, which lists merged pull requests and therefore says nothing at all
when a push was direct commits. Both scripts run locally, so you can see what a release
will say before it says it:

```bash
bash .github/scripts/next-version.sh
bash .github/scripts/release-notes.sh v0.2.0 v0.1.0
```

**The README is not automated.** Any `feat:` that changes what a player can do must
update the `## Features` section above **in the same branch**, in the existing style —
one short English bullet. A README-only sync uses `docs:`, and never carries
`[skip release]`.

## How it is put together

```
src/game/core/     the rules, the deadlock detector, the solver, the generator.
                   Plain TypeScript: no React, no DOM, no window. The same code
                   runs in the build script, in a Web Worker, and in Vitest.
src/game/session/  one game in progress: history, undo/redo, counters, the trail
src/game/storage/  localStorage, validated on the way in and unable to throw
src/game/levels/   the committed packs and their loader
src/game/workers/  the bridge from the UI to the generator
src/views/         Home and Play
scripts/           the pack generator, run by hand and committed
```

Two pieces carry most of the weight. The **solver** searches push space rather than move
space, and collapses every player position inside one reachable region into a single
state — without that collapse the same crate arrangement expands dozens of times and the
search runs out of budget before it reaches an answer. The **generator** builds a map,
places crates by pulling backwards from the won position, then asks the solver to score
the candidate and throws it away if the answer is wrong for the tier.

Undo restores a stored state rather than replaying the move backwards. Pulling is not the
inverse of pushing in Sokoban, and an inverse-move undo passes every test that never
pushes a crate before failing silently on the first one that does.

## Documentation

`docs/` holds the permanent documentation: what the product is and is not
([`01-product/overview.md`](docs/01-product/overview.md)), the user journeys, the function
inventory, the measurable thresholds, the architecture, and
[`03-design/invariants.md`](docs/03-design/invariants.md) — the things that break silently
and are worth reading before changing any of the code above.

Every technical decision with a real alternative is an ADR in
[`docs/decisions/`](docs/decisions/README.md): the design tokens, the static export and
the DOM board, the hybrid level generator, the solver, local storage, and this release
pipeline.
